[project]
name = "pyproject-subscriber"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = []

[tool.vercel]
entrypoint = "main:app"

[tool.vercel.subscribers.worker]
entrypoint = "worker:handle"
topics = ["tasks-topic"]
