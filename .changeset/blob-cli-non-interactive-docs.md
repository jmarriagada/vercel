---
---

Document non-interactive / agent use of the destructive `vercel blob` commands (`delete-store`, `empty-store`) in the Vercel CLI storage skill reference: they require `--yes` and fail fast with a structured `confirmation_required` error instead of prompting.
